'use strict';

/**
 * vendedor-pregunta service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendedor-pregunta.vendedor-pregunta');
